# effect-spacetimedb examples

Small, runnable examples for starting new `effect-spacetimedb` modules.

## Examples

- [`quickstart`](./quickstart/) - one public table, one reducer, and the module exports expected by the SpaceTimeDB module loader.
